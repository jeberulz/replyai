{
  "name": "@replypilot/extension",
  "version": "0.1.0",
  "private": true,
  "description": "ReplyPilot browser extension MVP — read-only score badge + deep link",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.5",
    "typescript": "^5"
  }
}
